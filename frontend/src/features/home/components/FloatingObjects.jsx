/**
 * FloatingObjects Component
 * 떠다니는 3D 물건들 컴포넌트
 */

import React, { memo } from 'react';

const FloatingObjects = memo(({ onCategoryClick }) => {
  // TODO: Three.js 3D 객체들 구현 예정
  // - 카메라, 드론, 캠핑용품 등의 3D 모델
  // - 마우스 인터랙션
  // - 클릭 시 카테고리 검색 이동
  
  return null;
});

FloatingObjects.displayName = 'FloatingObjects';

export default FloatingObjects;
