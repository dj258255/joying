# 어느 서버에 올릴지.
#
# 주소는 비밀이 아니지만 계정과 키 경로는 사람마다 다르다. 각자 환경에 맞게 고치거나
# --extra-vars 로 덮어쓴다.
[joying]
prod ansible_host=k13c202.p.ssafy.io

[joying:vars]
ansible_user=ubuntu
ansible_python_interpreter=/usr/bin/python3
