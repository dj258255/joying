# -------------------------------------
# 기본 애플리케이션 정보
# -------------------------------------
spring.application.name=Joying

# -------------------------------------
# 프로파일 설정
# -------------------------------------
spring.profiles.active=local

# -------------------------------------
# MySQL 데이터베이스 설정
# -------------------------------------
spring.config.activate.on-profile=local
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:project_db}?serverTimezone=Asia/Seoul
spring.datasource.username=${MYSQL_USERNAME:root}
spring.datasource.password=${MYSQL_PASSWORD:ssafy}

# -------------------------------------
# JPA 설정
# -------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# -------------------------------------
# JSON 변환 설정
# -------------------------------------
spring.jackson.property-naming-strategy=SNAKE_CASE

# -------------------------------------
# 파일 업로드 설정
# -------------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# -------------------------------------
# 기본 프로필 이미지 설정
# -------------------------------------
app.default-profile-image=/images/default_profile_image.png

# -------------------------------------
# Redis 설정
# -------------------------------------
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# -------------------------------------
# MongoDB 설정
# -------------------------------------
spring.data.mongodb.uri=mongodb://${MONGODB_USERNAME:joying}:${MONGODB_PASSWORD:joying_mongo_password}@${MONGODB_HOST:localhost}:${MONGODB_PORT:27017}/${MONGODB_DATABASE:joying_chat}?authSource=admin

# -------------------------------------
# 로깅 설정
# -------------------------------------
logging.level.org.hibernate.SQL=${LOGGING_LEVEL:debug}

# -------------------------------------
# Swagger/OpenAPI 설정
# -------------------------------------
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
springdoc.swagger-ui.operations-sorter=alpha
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.disable-swagger-default-url=true
springdoc.swagger-ui.doc-expansion=none
springdoc.default-consumes-media-type=application/json
springdoc.default-produces-media-type=application/json

# -------------------------------------
# JWT 설정
# -------------------------------------
jwt.secret=${JWT_SECRET:joying-local-secret-key-for-development-only}
jwt.access-token-expiration=${JWT_ACCESS_TOKEN_EXPIRATION:3600000}
jwt.refresh-token-expiration=${JWT_REFRESH_TOKEN_EXPIRATION:604800000}
jwt.access-token-cookie-name=${JWT_ACCESS_TOKEN_COOKIE_NAME:access_token}
jwt.refresh-token-cookie-name=${JWT_REFRESH_TOKEN_COOKIE_NAME:refresh_token}

# -------------------------------------
# OAuth2 설정
# -------------------------------------
oauth2.redirect-uri=${OAUTH2_REDIRECT_URI:http://localhost:3000/auth/callback}

# Kakao OAuth2
spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID:YOUR_KAKAO_CLIENT_ID}
spring.security.oauth2.client.registration.kakao.client-secret=${KAKAO_CLIENT_SECRET:YOUR_KAKAO_CLIENT_SECRET}
spring.security.oauth2.client.registration.kakao.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.scope=profile_nickname,account_email,profile_image
spring.security.oauth2.client.registration.kakao.client-name=Kakao
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post

# Kakao Provider
spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id

# -------------------------------------
# CORS 설정
# -------------------------------------
cors.allowed-origins=${FRONTEND_URL:http://localhost:3000}

# -------------------------------------
# 금융결제원 오픈뱅킹 API 설정
# -------------------------------------
openbanking.base-url=${OPENBANKING_BASE_URL:https://testapi.openbanking.or.kr}
openbanking.client-id=${OPENBANKING_CLIENT_ID:YOUR_OPENBANKING_CLIENT_ID}
openbanking.client-secret=${OPENBANKING_CLIENT_SECRET:YOUR_OPENBANKING_CLIENT_SECRET}
openbanking.redirect-uri=${OPENBANKING_REDIRECT_URI:http://localhost:8080/api/v1/accounts/callback}
openbanking.auth-uri=${OPENBANKING_AUTH_URI:https://testapi.openbanking.or.kr/oauth/2.0/authorize}
openbanking.token-uri=${OPENBANKING_TOKEN_URI:https://testapi.openbanking.or.kr/oauth/2.0/token}
openbanking.use-code=${OPENBANKING_USE_CODE:M202400001}