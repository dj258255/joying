# 거래 플로우 구현 계획서

## 📋 목차
1. [현재 플로우 분석](#1-현재-플로우-분석)
2. [원래 있어야 할 플로우](#2-원래-있어야-할-플로우)
3. [현재 상태와 비교](#3-현재-상태와-비교)
4. [구현 계획](#4-구현-계획)
5. [API 연동 계획](#5-api-연동-계획)
6. [컴포넌트 구현 계획](#6-컴포넌트-구현-계획)

---

## 1. 현재 플로우 분석

### 1.1 백엔드 상태 전환 (RentalStatus)
```
PENDING (결제 대기)
  ↓ 결제 완료
ESCROW (보증금 보관)
  ↓ 발송 (ship)
SHIPPED (발송 완료)
  ↓ 수령 확인 (confirmReceive)
RENTING (대여 중)
  ↓ 반납 (returnItem)
RETURN_REQUESTED (반납 요청)
  ↓ 회수 확인 (confirmReturn) + 자동 정산
RETURNED (회수 완료)
  ↓ (자동)
DEPOSIT_RETURNED (거래 완료 - 보증금 반환)
```

### 1.2 백엔드 API 엔드포인트 (이미 구현됨)
- ✅ `PATCH /rental-histories/{rentalHisId}/ship` - 발송 처리
- ✅ `PATCH /rental-histories/{rentalHisId}/confirm-receive` - 수령 확인 (SHIPPED → RENTING)
- ✅ `PATCH /rental-histories/{rentalHisId}/return` - 반납 처리 (RENTING → RETURN_REQUESTED)
- ✅ `PATCH /rental-histories/{rentalHisId}/confirm-return` - 회수 확인 + 자동 정산 (RETURN_REQUESTED → RETURNED → DEPOSIT_RETURNED)
- ✅ `POST /rental-cancels` - 거래 취소 요청

### 1.3 현재 프론트엔드 구현 상태

#### ✅ 구현된 부분
1. **발송 플로우**
   - `ShippingModal`: 영상 녹화 + 운송장 등록
   - `ShippingMessageCard`: 발송 메시지 카드 + 배송 조회 버튼
   - 발송 완료 시 채팅 메시지: "📦 물품을 발송했습니다!"

2. **수령 확인 플로우 (부분적)**
   - `ReceiveModal`: 영상 녹화 + 수령 확인 API 호출
   - `MessageBubble` 내부에 "✅ 물품 수령 확인하기" 버튼 (2325-2351 라인)
   - 수령 완료 시 채팅 메시지: "✅ 물품을 수령했습니다!"

3. **반납 수령 확인 플로우 (부분적)**
   - `ReturnReceiveModal`: 영상 녹화 + 회수 확인 API 호출 (자동 정산 포함)
   - `MessageBubble` 내부에 "✅ 반납 수령 확인하기" 버튼 (2409-2429 라인)

4. **거래 프로세스 모달**
   - `TransactionProcessModal`: 전체 거래 프로세스 확인 가능
   - "거래 보기" 버튼으로 접근

#### ❌ 미구현/개선 필요한 부분
1. **수령 확인 플로우**
   - ✅ `MessageBubble` 내부에 "물품 수령 확인하기" 버튼 있음 (2324-2351 라인)
   - ❌ `ShippingMessageCard`에 "물품 수령 확인" 버튼 없음 (카드 형태로 개선 필요)
   - ❌ 수령 확인 메시지가 카드 형태가 아님 (MessageBubble 내부 버튼만 있음)
   - ✅ 수령 완료 후 "반납하기", "거래 중단하기" 버튼 있음 (2357-2391 라인)
   - ❌ 수령 완료 메시지가 카드 형태가 아님 (MessageBubble 내부 버튼만 있음)

2. **반납 플로우**
   - ✅ `TransactionProcessModal`에 반납 처리 기능 구현됨 (587-632 라인)
   - ✅ 반납 완료 메시지 전송 기능 있음 (611-620 라인)
   - ✅ `MessageBubble` 내부에 "반납 수령 확인하기" 버튼 있음 (2394-2438 라인)
   - ❌ 반납 완료 메시지 카드가 없음 (MessageBubble 내부 버튼만 있음)
   - ❌ 반납 배송 조회 기능이 카드 형태가 아님

3. **거래 프로세스 모달**
   - ✅ 반납 단계 구현됨 (1232-1316 라인)
   - ✅ 회수 확인 기능 있음 (635-652 라인)
   - ⚠️ 거래 취소 기능이 모달 내부에 있지만 더 강화 가능

---

## 2. 원래 있어야 할 플로우

### 2.1 전체 거래 플로우
```
1. 대여 요청
   └─ 대여 요청 메시지 카드
   └─ "거래 생성하기" 버튼 (판매자)

2. 거래 생성 완료
   └─ 거래 생성 완료 메시지 카드
   └─ "결제하러 가기" 버튼 (구매자)

3. 결제 완료
   └─ 결제 완료 메시지 카드
   └─ "물품 보내기" 버튼 (판매자)

4. 물품 발송
   └─ 발송 메시지 카드 (ShippingMessageCard)
   └─ "배송 조회" 버튼 (양쪽)
   └─ "물품 수령 확인" 버튼 (구매자) ← 현재 없음

5. 물품 수령 확인
   └─ 수령 확인 메시지 카드 ← 새로 만들기
   └─ "반납하기" 버튼 (구매자) ← 새로 만들기
   └─ "거래 중단하기" 버튼 (구매자) ← 새로 만들기

6. 반납
   └─ 반납 메시지 카드 ← 새로 만들기
   └─ "반납 배송 조회" 버튼 (양쪽) ← 새로 만들기
   └─ "반납 수령 확인" 버튼 (판매자)

7. 반납 수령 확인
   └─ 반납 수령 확인 메시지 카드
   └─ 자동 정산 처리 (백엔드)

8. 거래 완료
   └─ 거래 완료 메시지
```

### 2.2 거래 보기 (TransactionProcessModal) 기능
- 전체 거래 프로세스 확인
- 현재 단계 강조 표시
- 각 단계별 액션 버튼
- 거래 취소 기능 (각 단계에서 가능한 경우)

---

## 3. 현재 상태와 비교

### 3.1 차이점 분석

| 단계 | 원래 있어야 할 플로우 | 현재 상태 | 차이점 |
|------|---------------------|----------|--------|
| 발송 | 발송 카드 + 배송 조회 + **수령 확인 버튼** | 발송 카드 + 배송 조회만 | 수령 확인 버튼 없음 |
| 수령 확인 | 수령 카드 + 반납하기 + 거래 중단하기 | MessageBubble 버튼만 | 카드 형태 아님 |
| 반납 | 반납 모달 + 반납 카드 + 반납 배송 조회 | 반납 모달 없음 | 반납 플로우 미구현 |
| 반납 수령 | 반납 수령 카드 + 자동 정산 | MessageBubble 버튼만 | 카드 형태 아님 |
| 거래 보기 | 프로세스 확인 + 취소 가능 | 프로세스 확인만 | 취소 기능 부족 |

---

## 4. 구현 계획

### 4.1 Phase 1: 물품 발송 후 수령 확인 플로우

#### 4.1.1 ShippingMessageCard 수정
**파일**: `frontend/src/features/chat/components/ShippingMessageCard.jsx`

**변경 사항**:
1. "물품 수령 확인" 버튼 추가
   - 조건: `status === 'SHIPPED'` && `isOwn === false` (구매자만)
   - 클릭 시: `ReceiveModal` 열기

**코드 위치**:
- 154번 라인 근처 ("배송 조회" 버튼 아래)

```javascript
// 배송 조회 버튼 아래에 추가
{!isOwn && shippingInfo.status === 'SHIPPED' && (
  <button
    onClick={handleReceiveClick}
    className="glass-button w-full text-sm mt-2 py-2.5"
  >
    물품 수령 확인
  </button>
)}
```

#### 4.1.2 ReceiveMessageCard 생성 (새 파일)
**파일**: `frontend/src/features/chat/components/ReceiveMessageCard.jsx`

**기능**:
- 수령 확인 메시지를 카드 형태로 표시
- "반납하기" 버튼 (구매자)
- "거래 중단하기" 버튼 (구매자)

**메시지 파싱**:
- 패턴: "✅ 물품을 수령했습니다!"
- rentalHisId 추출: `rentalHisId:(\d+)`

**버튼 동작**:
- "반납하기": 반납 모달 열기
- "거래 중단하기": 거래 취소 모달 열기

#### 4.1.3 ChatRoomPage 수정
**파일**: `frontend/src/features/chat/pages/ChatRoomPage.jsx`

**변경 사항**:
1. `ReceiveMessageCard` import 및 렌더링 로직 추가
   - 2682번 라인 근처 (`ShippingMessageCard` 렌더링 아래)
   - 메시지 내용에서 "✅ 물품을 수령했습니다" 패턴 감지

2. 수령 확인 후 메시지 전송 로직 유지 (이미 구현됨)
   - 3100-3124번 라인

3. 반납 모달 상태 추가
   ```javascript
   const [showReturnModal, setShowReturnModal] = useState(false);
   ```

---

### 4.2 Phase 2: 반납 플로우

#### 4.2.1 ReturnModal (이미 TransactionProcessModal에 구현됨)
**파일**: `frontend/src/features/chat/components/TransactionProcessModal.jsx`

**현재 상태**:
- ✅ 반납 처리 기능 구현됨 (587-632 라인)
- ✅ 반납 완료 메시지 전송 기능 있음 (611-620 라인)
- ✅ 영상 녹화 지원 (VideoRecorder 연동)
- ✅ 운송장 입력 UI 있음 (1240-1267 라인)

**개선 필요**:
- ❌ 별도 `ReturnModal` 컴포넌트는 없지만, `TransactionProcessModal`의 반납 단계를 활용
- ❌ 채팅방에서 직접 반납 모달을 열 수 있는 버튼 필요 (현재는 거래 보기 모달 내부에서만 가능)

#### 4.2.2 ReturnMessageCard 생성 (새 파일)
**파일**: `frontend/src/features/chat/components/ReturnMessageCard.jsx`

**기능**:
- 반납 완료 메시지를 카드 형태로 표시
- "반납 배송 조회" 버튼 (양쪽)
- "반납 수령 확인" 버튼 (판매자만)

**메시지 파싱**:
- 패턴: "📦 반납을 완료했습니다!"
- rentalHisId 추출
- 택배사, 운송장 번호 추출

**배송 조회**:
- `TrackingStatusCard` 사용 (현재 발송 배송 조회와 동일)
- `returnCarrierCode`, `returnTrackingNo` 사용

#### 4.2.3 ChatRoomPage 수정

**변경 사항**:
1. `ReturnModal` import 및 상태 관리
   ```javascript
   import ReturnModal from '../../../features/rental/components/ReturnModal';
   const [showReturnModal, setShowReturnModal] = useState(false);
   ```

2. `ReturnMessageCard` import 및 렌더링 로직 추가
   - "📦 반납을 완료했습니다" 패턴 감지

3. 반납 완료 후 메시지 전송
   ```javascript
   const messageContent = `📦 반납을 완료했습니다!\n\n택배사: ${courier}\n운송장 번호: ${trackingNo}\n\n${videoUrl ? `[동영상 보기](${videoUrl})` : ''}\nrentalHisId:${currentRentalData.rentalHisId}`;
   ```

4. `ReceiveMessageCard`에서 "반납하기" 버튼 클릭 시
   - `ReturnModal` 열기

---

### 4.3 Phase 3: 거래 보기 모달 개선

#### 4.3.1 TransactionProcessModal 수정
**파일**: `frontend/src/features/chat/components/TransactionProcessModal.jsx`

**변경 사항**:
1. 거래 취소 기능 추가
   - 각 단계에서 취소 가능 여부 확인
   - "거래 취소하기" 버튼 추가
   - `CancelRequestModal` 연동

2. 현재 단계 강조 표시
   - 시각적 표시 개선

3. 반납 단계 추가
   - `return` 단계 추가
   - 반납 배송 조회 기능 추가

---

## 5. API 연동 계획

### 5.1 수령 확인 API
**엔드포인트**: `PATCH /rental-histories/{rentalHisId}/confirm-receive`

**요청**:
```json
{}
```

**응답**:
```json
{
  "status": 200,
  "message": "OK",
  "data": {
    "rentalHisId": 26,
    "status": "RENTING",
    "message": "수령이 확인되었습니다. 대여가 시작되었습니다"
  }
}
```

**연동 위치**:
- `ReceiveModal.jsx` - 이미 구현됨 (203번 라인)

---

### 5.2 반납 API
**엔드포인트**: `PATCH /rental-histories/{rentalHisId}/return`

**요청**:
```json
{
  "carrierCode": "CJ",
  "trackingNo": "123456789012"
}
```

**응답**:
```json
{
  "status": 200,
  "message": "OK",
  "data": {
    "rentalHisId": 26,
    "status": "RETURN_REQUESTED",
    "carrierCode": "CJ",
    "trackingNo": "123456789012",
    "message": "반납이 완료되었습니다"
  }
}
```

**연동 위치**:
- `ReturnModal.jsx` (새로 생성)
- `rentalApi.returnItem()` 호출

---

### 5.3 회수 확인 API (자동 정산 포함)
**엔드포인트**: `PATCH /rental-histories/{rentalHisId}/confirm-return`

**요청**:
```json
{}
```

**응답**:
```json
{
  "status": 200,
  "message": "OK",
  "data": {
    "rentalHisId": 26,
    "status": "DEPOSIT_RETURNED",
    "message": "회수가 확인되었습니다. 자동 정산이 완료되었습니다 (대여료 지급 + 보증금 반환)"
  }
}
```

**정산 처리**:
- 백엔드에서 자동 처리됨 (`RentalService.confirmReturn()`)
- 대여료 지급 + 보증금 반환

**연동 위치**:
- `ReturnReceiveModal.jsx` - 이미 구현됨 (220번 라인)

---

### 5.4 거래 취소 API
**엔드포인트**: `POST /rental-cancels`

**요청**:
```json
{
  "rentalHisId": 26,
  "reason": "거래 중단 사유",
  "cancelType": "BUYER_CANCEL" | "SELLER_CANCEL"
}
```

**연동 위치**:
- `TransactionProcessModal.jsx`
- `CancelRequestModal.jsx` (이미 구현됨)

---

## 6. 컴포넌트 구현 계획

### 6.1 새로 생성할 컴포넌트

#### 6.1.1 ReceiveMessageCard.jsx
```
frontend/src/features/chat/components/ReceiveMessageCard.jsx
```

**Props**:
- `message`: 메시지 객체
- `isOwn`: 본인 메시지 여부
- `onReturnClick`: 반납하기 버튼 클릭 핸들러
- `onCancelClick`: 거래 중단하기 버튼 클릭 핸들러

**기능**:
- 수령 확인 메시지 파싱
- 카드 형태로 표시
- "반납하기", "거래 중단하기" 버튼

#### 6.1.2 ReturnModal (별도 생성 불필요)
**현재 상태**: `TransactionProcessModal`의 반납 단계로 이미 구현됨

**선택사항**: 
- `ShippingModal`과 같은 독립적인 모달 컴포넌트를 만들 수도 있지만, 현재 `TransactionProcessModal`로 충분히 동작함
- 다만, 채팅방에서 바로 반납 모달을 열 수 있도록 버튼 연결 필요

#### 6.1.3 ReturnMessageCard.jsx
```
frontend/src/features/chat/components/ReturnMessageCard.jsx
```

**Props**:
- `message`: 메시지 객체
- `isOwn`: 본인 메시지 여부
- `onTrackClick`: 반납 배송 조회 버튼 클릭 핸들러
- `onReceiveConfirmClick`: 반납 수령 확인 버튼 클릭 핸들러

**기능**:
- 반납 완료 메시지 파싱
- 카드 형태로 표시
- "반납 배송 조회", "반납 수령 확인" 버튼

---

### 6.2 수정할 컴포넌트

#### 6.2.1 ShippingMessageCard.jsx
**변경 사항**:
- "물품 수령 확인" 버튼 추가
- Props에 `onReceiveClick` 추가 (또는 내부에서 직접 모달 열기)

#### 6.2.2 ChatRoomPage.jsx
**변경 사항**:
1. Import 추가:
   ```javascript
   import ReceiveMessageCard from '../components/ReceiveMessageCard';
   import ReturnMessageCard from '../components/ReturnMessageCard';
   ```
   - `ReturnModal`은 별도 생성 불필요 (`TransactionProcessModal` 사용)

2. 메시지 렌더링 로직 수정:
   - `ReceiveMessageCard` 렌더링 (수령 확인 메시지)
   - `ReturnMessageCard` 렌더링 (반납 완료 메시지)
   - 현재 `MessageBubble` 내부 버튼 대신 카드 형태로 변경

3. `ShippingMessageCard`에 "물품 수령 확인" 버튼 추가:
   - 조건: `status === 'SHIPPED'` && `isOwn === false`
   - 클릭 시: `ReceiveModal` 열기

#### 6.2.3 TransactionProcessModal.jsx
**변경 사항**:
1. 반납 단계 추가
2. 거래 취소 기능 강화
3. 현재 단계 시각적 강조

---

## 7. 구현 순서

### 7.1 1단계: 수령 확인 플로우 완성
1. ✅ `ReceiveModal` 확인 (이미 구현됨)
2. ❌ `ShippingMessageCard`에 "물품 수령 확인" 버튼 추가
3. ❌ `ReceiveMessageCard` 생성
4. ❌ `ChatRoomPage`에 `ReceiveMessageCard` 렌더링 로직 추가

### 7.2 2단계: 반납 플로우 구현
1. ❌ `ReturnModal` 생성
2. ❌ `ReturnMessageCard` 생성
3. ❌ `ChatRoomPage`에 반납 플로우 통합
4. ❌ 반납 완료 후 메시지 전송 로직 추가

### 7.3 3단계: 거래 보기 모달 개선
1. ✅ `TransactionProcessModal`에 반납 단계 이미 구현됨 (1232-1316 라인)
2. ⚠️ 거래 취소 기능 추가/강화 가능
3. ⚠️ UI/UX 개선 가능

---

## 8. 예상 작업 시간

| 단계 | 작업 내용 | 예상 시간 |
|------|----------|----------|
| 1단계 | 수령 확인 플로우 카드화 | 2-3시간 |
| 2단계 | 반납 플로우 카드화 | 2-3시간 |
| 3단계 | 거래 보기 모달 개선 (선택) | 1-2시간 |
| **총계** | | **5-8시간** |

**참고**: 대부분의 로직이 이미 구현되어 있어, UI를 카드 형태로 개선하는 작업이 주 목표

---

## 9. 주의사항

### 9.1 백엔드 API
- ✅ 모든 필요한 API가 이미 구현되어 있음
- ✅ 보증금 반환은 회수 확인 시 자동 처리됨

### 9.2 상태 관리
- `rentalHisId`를 메시지 내용에서 파싱하는 로직 유지
- 각 단계별 상태 확인 로직 추가 필요

### 9.3 UI/UX
- 기존 카드 컴포넌트 (`ShippingMessageCard`, `PaymentCompleteMessageCard`)와 디자인 통일
- 검정/흰색 글래스모피즘 디자인 유지

### 9.4 테스트
- 각 단계별 버튼 동작 확인
- API 호출 성공/실패 케이스 처리
- 에러 핸들링

---

## 10. 추가 고려사항

### 10.1 반납 배송 조회
- 현재 배송 조회 API와 동일하게 사용
- `returnCarrierCode`, `returnTrackingNo` 사용
- `TrackingStatusCard` 재사용

### 10.2 거래 취소
- 각 단계에서 취소 가능 여부 확인
- 취소 사유 입력 모달 (`CancelRequestModal`) 활용
- 취소 승인/거부 프로세스 확인

### 10.3 영상 업로드
- 각 단계별 videoType 확인:
  - 발송: `OWNER_SEND`
  - 수령: `RENTER_RECEIVE`
  - 반납: `RENTER_RETURN`
  - 반납 수령: `LENDER_RECEIVE`

---

**작성일**: 2025-11-14  
**작성자**: AI Assistant  
**검토 필요**: 백엔드 API 확인, 프론트엔드 컴포넌트 구조 확인

